import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors - Minimal & Professional
  static const Color primary = Color(0xFF1A1A1A);        // Dark charcoal
  static const Color secondary = Color(0xFF4A4A4A);      // Medium gray
  static const Color accent = Color(0xFF2D2D2D);         // Slightly lighter charcoal
  
  // Background Colors
  static const Color background = Color(0xFFFAFAFA);     // Off-white
  static const Color surface = Color(0xFFFFFFFF);        // Pure white
  static const Color cardBackground = Color(0xFFF5F5F5); // Light gray
  
  // Text Colors
  static const Color textPrimary = Color(0xFF1A1A1A);    // Dark text
  static const Color textSecondary = Color(0xFF757575);  // Gray text
  static const Color textHint = Color(0xFFBDBDBD);       // Light gray
  
  // Functional Colors
  static const Color error = Color(0xFFD32F2F);          // Red
  static const Color success = Color(0xFF388E3C);        // Green
  static const Color warning = Color(0xFFF57C00);        // Orange
  
  // Border & Divider
  static const Color border = Color(0xFFE0E0E0);         // Light border
  static const Color divider = Color(0xFFEEEEEE);        // Very light divider
  
  // Icon Colors
  static const Color iconPrimary = Color(0xFF1A1A1A);
  static const Color iconSecondary = Color(0xFF9E9E9E);
}